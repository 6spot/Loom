# Loom Applications

User-facing Loom Applications live here.

The official UI direction is GPUI, with the goal of sharing Rust UI code across native targets and the emerging GPUI Web/WASM backend. The first UI crate will be added only after we pin and validate a concrete GPUI upstream revision; Loom Core does not depend on that choice.
