[package]
name = "loom-validator"
version.workspace = true
edition.workspace = true

[dependencies]
loom-client = { path = "../../crates/loom-client" }
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true

[dev-dependencies]
serde_json = "1"
