[package]
name = "loom-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
loom-core = { path = "../loom-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
