[package]
name = "loom-storage"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
test-support = ["dep:url"]

[dependencies]
blake3 = "1"
loom-core = { path = "../loom-core", version = "0.1.0" }
loom-runtime = { path = "../loom-runtime", version = "0.1.0" }
object_store = { version = "0.14", default-features = true }
serde_json = "1"
sqlx = { version = "0.9", default-features = false, features = ["json", "macros", "migrate", "postgres", "runtime-tokio", "tls-rustls", "uuid"] }
url = { version = "2", optional = true }

[dev-dependencies]
loom-agency = { path = "../loom-agency", version = "0.1.0" }
loom-api = { path = "../loom-api", version = "0.1.0" }
loom-capability = { path = "../loom-capability", version = "0.1.0" }
loom-protocol = { path = "../loom-protocol", version = "0.1.0" }
semver = "1"
tokio = { version = "~1.51", features = ["macros", "rt-multi-thread"] }
url = "2"

[lints]
workspace = true
