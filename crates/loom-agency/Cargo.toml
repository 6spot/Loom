[package]
name = "loom-agency"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
loom-core = { path = "../loom-core", version = "0.1.0" }
loom-protocol = { path = "../loom-protocol", version = "0.1.0" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
